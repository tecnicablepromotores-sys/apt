package com.example.data

import com.google.firebase.database.FirebaseDatabase
import com.google.firebase.firestore.FirebaseFirestore

object DataSeeder {
    suspend fun seedAllData(db: FirebaseDatabase, firestore: FirebaseFirestore) {
        // Funcionalidad de datos de prueba desactivada a peticion del usuario.
    }
}

